# Personal Planner Bot — ТЗ

## Що це

Особистий Telegram-бот для одного користувача. Python + aiogram 3 + PostgreSQL + APScheduler. Без бекенду, без фронту, без ролей — просто бот і база.

## Стек

- Python 3.12
- aiogram 3.x
- PostgreSQL (через `asyncpg` + `SQLAlchemy async`)
- Alembic (міграції)
- APScheduler
- Railway деплой (Dockerfile)

## Структура файлів

```
planner-bot/
├── main.py
├── config.py
├── database.py
├── models.py
├── handlers/
│   ├── __init__.py
│   ├── add.py
│   ├── today.py
│   ├── done.py
│   ├── upcoming.py
│   └── settings.py
├── services/
│   └── scheduler.py
├── alembic/
│   └── versions/
├── alembic.ini
├── Dockerfile
├── .env.example
└── requirements.txt
```

## База даних (PostgreSQL)

### Таблиця `items`

| Поле | Тип | Опис |
|---|---|---|
| id | SERIAL PK | |
| type | TEXT | `task` або `event` |
| title | TEXT | назва |
| date | DATE | для події обов'язкова, для задачі опціональна |
| time | TIME | опціональний, тільки для події |
| done | BOOLEAN | тільки для задач, default false |
| created_at | TIMESTAMP | |

### Таблиця `settings`

| Поле | Тип | Опис |
|---|---|---|
| key | TEXT PK | |
| value | TEXT | |

Зберігає: `morning_time` (default `08:00`), `timezone` (default `Europe/Kyiv`).

## Reply Keyboard

Постійна клавіатура знизу, завжди видима:

```
[ ☀️ Сьогодні ]  [ ➕ Додати    ]
[ ✅ Виконано  ]  [ 📅 Найближче ]
[ ⚙️ Налаштування ]  [ ❓ Допомога ]
```

Кожна кнопка — текстовий тригер, обробляється як відповідна команда. Команди (`/today`, `/add` і т.д.) теж працюють паралельно.

## Флоу "➕ Додати"

```
→ бот: "Що додаємо?" [📋 Задача] [📅 Подія]  ← inline кнопки

Якщо 📋 Задача:
→ "Назва?"
→ юзер вводить текст
→ "На коли? (ДД.ММ або /skip)"
→ збережено ✅

Якщо 📅 Подія:
→ "Назва?"
→ юзер вводить текст
→ "Дата? (ДД.ММ або ДД.ММ.РРРР)"
→ "Час? (ГГ:ХХ або /skip)"
→ збережено ✅
→ scheduler автоматично ставить нагадування за 1 годину (якщо є час)
```

## Флоу "☀️ Сьогодні"

```
☀️ Сьогодні, 23 травня

⚠️ Прострочено:
• Зателефонувати бухгалтеру (вчора)  [✅]
• Здати документи (3 дні тому)  [✅]

📅 Події:
• 10:00 — Зустріч з дизайнером
• 15:30 — Лікар

📋 На сьогодні:
• Написати пост в LinkedIn
• Переглянути контракт
```

Прострочені — задачі з датою в минулому, не виконані. Inline кнопка `[✅]` поруч — відмітити виконаною прямо звідси. Секція "Прострочено" йде першою.

Якщо нічого немає — "Сьогодні нічого не заплановано 🎉"

## Ранковий briefing

Те саме що "☀️ Сьогодні" — надсилається автоматично о часі з налаштувань. Прострочені теж з inline кнопками `[✅]`.

## Флоу "✅ Виконано"

Список незавершених задач (сьогодні + без дати). Inline кнопки — тапнув, закрито.

## Флоу "📅 Найближче"

```
📅 Найближчі 7 днів

24 травня
• 14:00 — Дзвінок з інвестором

26 травня
• День народження мами

28 травня
• 09:00 — Виліт
```

## Нагадування про події

При збереженні події з часом — APScheduler автоматично ставить job на `event_time - 1 година`. Надсилає:

```
⏰ Нагадування: через 1 годину — Зустріч з дизайнером (10:00)
```

Якщо час події не вказаний — нагадування не створюється.

## Флоу "⚙️ Налаштування"

```
⚙️ Налаштування
Briefing зараз: 08:00

[🕐 Змінити час briefing]
→ "Введи час (ГГ:ХХ):"
→ юзер: 07:30
→ "✅ Briefing о 07:30"
→ scheduler перезапускає job
```

## Флоу "❓ Допомога"

Просте повідомлення з описом усіх кнопок.

## Безпека

`ALLOWED_USER_ID` з `.env` — бот реагує тільки на цей ID, решта ігнорується мовчки.

## .env

```env
BOT_TOKEN=
ALLOWED_USER_ID=
DATABASE_URL=postgresql+asyncpg://user:pass@host:5432/planner
```

## Dockerfile

```dockerfile
FROM python:3.12-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
CMD ["python", "main.py"]
```

## Що не входить

- Редагування записів (видалив і додав заново)
- Повторювані події
- Налаштування часу нагадування
- Веб-інтерфейс
